# Chapter 4：函数接口与程序拆分

- 按值传递会创建参数副本，适合小对象或函数需要取得独立值的场景。
- `const T&` 避免昂贵复制，并保证函数不通过该引用修改对象。
- `T&` 表示函数必须修改调用者对象。
- 重载函数共享名字，但参数列表必须可区分；不能只靠返回类型重载。

`median(std::vector<double> values)` 有意按值接收，因为排序副本不能改变调用者的作业成绩。`read_hw` 通过非常量引用写入容器，并返回原输入流以支持状态检查。

异常使用 `const std::exception&` 捕获，避免复制和异常对象切片。
